name: Release

on:
  push:
    tags: ["v*.*.*"]

permissions: {}

jobs:
  release:
    name: Release
    uses: grafana/xk6/.github/workflows/tooling-release.yml@15b66b652a3c087aabe41c5b09e2678515842344 # v1.3.5
    permissions:
      contents: write
      packages: write
      id-token: write
    with:
      go-version: "1.25.x"
      goreleaser-version: "2.8.2"
      k6-versions: '["v1.2.3","v1.0.0"]'